{
  "name": "envguard",
  "version": "1.1.0",
  "description": "🔒 Validate .env files, detect secrets, keep env configs in sync",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "envguard": "./dist/index.cjs"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsup",
    "test": "node --test tests/standalone.test.mjs",
    "test:watch": "node --test --watch tests/standalone.test.mjs",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "test:standalone": "node --test tests/standalone.test.mjs"
  },
  "dependencies": {
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "eslint": "^8.57.1",
    "tsup": "^8.4.0",
    "tsx": "^4.22.4",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  },
  "keywords": [
    "env",
    "dotenv",
    "validate",
    "secrets",
    "cli",
    "security"
  ]
}
