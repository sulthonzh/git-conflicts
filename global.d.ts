// Global type definitions for git-conflicts
// Note: simple-git module declarations are in src/simple-git.d.ts (single source of truth)
// Note: ProgressTracker, ConflictStatus, FileConflictStatus types are defined as concrete
// implementations in their respective source files — no global declarations needed.
