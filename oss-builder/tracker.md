# OSS Builder Tracker

- [x] **eventkit-x** (2026-06-15) — Zero-dependency event system with advanced features. **EventEmitter**: Priority handling, wildcard events (*), event filtering, async/sync support, once/on listeners, statistics tracking, error handling, max listeners configuration. **EventGroup**: Concurrency control (maxConcurrent), error handling, grouped event emission, operation tracking. **EventMiddleware**: Transform, validate, throttle, debounce, logging middleware, context manipulation, chain processing, stop propagation. Features: createEventBus factory, predefined middleware utilities, TypeScript support, comprehensive test coverage (58 tests), zero dependencies. GitHub: https://github.com/sulthonzh/eventkit-x.

## Shipped
- [x] **circuit-breaker-x** (2026-06-15) — Zero-dependency circuit breaker pattern for resilient async applications. State machine (CLOSED→OPEN→HALF_OPEN→CLOSED), sliding window counters, HALF_OPEN trial calls, fallback function support, event monitoring, comprehensive metrics, 9 tests, zero dependencies. GitHub: https://github.com/sulthonzh/circuit-breaker-x.
- [x] **async-lock-x** (2026-06-15) — Zero-dep async synchronization primitives: Mutex (FIFO mutual exclusion, runExclusive auto-release), Semaphore (counting lock, tryAcquire non-blocking), ReadWriteLock (multiple concurrent readers + exclusive writer, writer preference), Barrier (N-party sync point, reusable). 27 tests, zero deps. GitHub: https://github.com/sulthonzh/async-lock-x.
- [x] **git-squash-preview** (2026-06-15) — Zero-dep TAP output parser and git squash preview. Parse TAP v13 with ok/not ok, plans, SKIP/TODO directives, YAML diagnostics, bail out, pragmas, subtests. Results summary with timing analysis, color-coded output, JSON export. CLI (--input, --timing, --summary, --verbose, --json). TypeScript ES modules, 3-file architecture, comprehensive tests. GitHub: https://github.com/sulthonzh/git-squash-preview.
- [x] **query-string-x** (2026-06-15) — Zero-dep URL query string parser and manipulator. Parse/stringify with type inference (numbers, booleans, dates), nested objects (dot notation), array formats (brackets/repeat/comma), URL parsing/building, query manipulation (merge/pick/omit/get/set), comprehensive CLI tool. 57 tests, zero dependencies. GitHub: https://github.com/sulthonzh/query-string-x.
- [x] **bitset-x** (2026-06-15) — Zero-dep Uint32Array-backed bitset. O(1) set/get/clear/toggle. Bitwise returning new Bitset: and/or/xor/not/andNot(difference). In-place variants: andInPlace/orInPlace/xorInPlace. Range ops: setRange/clearRange/toggleRange/countRange. Bit scanning: nextSetBit/prevSetBit/nextClearBit. Comparison: intersects/isSubsetOf/isSupersetOf/equals. Symbol.iterator, Serialization: toJSON/fromJSON, toBinaryString/fromBinaryString, toHexString. Factory: from(indices)/fromBinaryString. Utils: popcount32/ctz32. CLI (set/get/info/and/or/xor/demo). 52 tests, zero deps. GitHub: https://github.com/sulthonzh/bitset-x.
- [x] **ringbuffer-x** (2026-06-15) — Zero-dep circular ring buffer. Two overflow modes: reject (throws when full) and overwrite (evicts oldest, returns it). O(1) push/pop/popBack, peek/peekBack, get/set by logical index, drain(n), clear. Search: find/findIndex/includes. Full iteration: Symbol.iterator, forEach, map. Serialization: toJSON/fromJSON. Static from(iterable, capacity) keeps last N on overflow. Stateful CLI (push/pop/peek/info/drain/demo/from-json/clear). 44 tests, zero deps. GitHub: https://github.com/sulthonzh/ringbuffer-x.
- [x] **avl-tree-x** (2026-06-15) — Zero-dep AVL self-balancing BST. Insert/delete/search in O(log n) with 4 rotation cases (LL, RR, LR, RL). Range queries [low, high], predecessor/successor, rank (count < target), select (k-th smallest). Traversals: in/pre/post/level-order. Symbol.iterator. Custom comparators. toJSON/fromJSON. isValid() checks BST + AVL properties. CLI (insert/search/range/rank/select/order/info/demo). 50 tests, zero deps. GitHub: https://github.com/sulthonzh/avl-tree-x.
- [x] **fsm-x** (2026-06-15) — Zero-dep finite state machine library. Hierarchical (nested) states with automatic initial-child resolution, guarded transitions (boolean predicates), entry/exit/transition actions, internal transitions (no target = stay in place), wildcard `*` event matching, final states (type:'final' auto-stops machine), lifecycle hooks (on('transition'/'state'/'event'/'error') with unsubscribe), availableEvents() (collects from current state up ancestor chain), can() (checks transition existence + guard evaluation), matches() (hierarchical prefix: matches('active') true when in 'active.running'), history states (.H shallow / .H* deep), toJSON/fromJSON persistence, Mermaid state diagram export + Graphviz DOT export. CLI (run/events/info/mermaid/dot/demo). 60 tests, zero deps. GitHub: https://github.com/sulthonzh/fsm-x.
- [x] **validatekit** (2026-06-15) — Zero-dep validation library. 100+ validators across 5 categories: type checks (30+: null/undefined/string/number/date/map/set/promise/class/async/generator/etc), number checks (integer/float/prime/even/odd/powerOfTwo/perfectSquare/inRange/between/divisibleBy), string checks (alpha/alphanumeric/slug/hex/base64/base32/palindrome/ascii/multibyte/length), format checks (email/url/ipv4/ipv6/uuid/uuidv4/uuidv7/hexColor/rgbColor/hslColor/json/iso/creditCard-Luhn/isbn/issn/ean/mac/imei/iban-Luhn/jwt/semVer/mimeType/dataUri/phone/postalCode-multiple-countries/currency/lat-lng), composition utilities (compose/anyOf/allOf/noneOf/oneOf/optional/nullable/withDefault/not/validate-schema). Currying support for multi-arg validators. CLI (check/list/demo). 110 tests, zero deps. GitHub: https://github.com/sulthonzh/validatekit.
- [x] **trie-x** (2026-06-15) — Zero-dep trie (prefix tree). Insert/search/startsWith with O(L) per op, insertAll chaining, remove with automatic dead-node pruning (keeps shared prefix nodes), autocomplete (sorted results + optional limit), wildcard pattern matching (? = single char, * = any sequence including empty — both combinable), longestPrefixOf (longest stored word that is prefix of input — for tokenizers/routers/input completion), getAllWords, countNodes, size/isEmpty/clear, toJSON/fromJSON serialization (compact {e,c} format), Trie.from() static factory. CLI (insert/search/prefix/complete/wildcard/longest/words/info/demo, stdin JSON array or newline-delimited, --limit/--json). 37 tests, zero deps. GitHub: https://github.com/sulthonzh/trie-x.
- [x] **graphlib-x** (2026-06-15) — Zero-dep graph algorithms library. Directed/undirected weighted graphs (Graph class: add/remove nodes+edges, neighbors, degree/inDegree/outDegree, clone, toJSON/fromJSON). 20+ algorithms: **Traversal**: BFS (distances + parent tracking, visit callback skip), DFS (discover/finish timestamps), dfsAll (all components), reconstructPath. **Shortest paths**: Dijkstra (linear min-scan), shortestPath, Bellman-Ford (negative weights + negative cycle detection), Floyd-Warshall (all-pairs O(V³)), A* (custom heuristic). **Topological sort**: Kahn's (BFS in-degree) + DFS-based, cycle → null. **Cycle detection**: directed (3-color DFS), undirected (union-find), findCycle (reconstructs cycle path). **Components**: connectedComponents (BFS, weakly connected for directed), stronglyConnectedComponents (Tarjan's single-pass). **MST**: kruskalMST (union-find), primMST (greedy nearest). **Max flow**: Ford-Fulkerson / Edmonds-Karp (BFS augmenting paths, residual capacities). **Bipartite**: isBipartite (2-coloring BFS) with coloring map. **Centrality**: betweennessCentrality (Brandes' O(VE) algorithm). CLI (demo/info/bfs/dfs/topo/cycle/components/scc/dijkstra/path/mst/flow/bipartite/centrality, --input/--json). 46 tests, zero deps. GitHub: https://github.com/sulthonzh/graphlib-x.
- [x] **bloom-filter** (2026-06-15) — Zero-dep probabilistic set membership testing. Three variants: **BloomFilter** (standard, bit-packed Uint8Array with optimal sizing from capacity+errorRate, double hashing via Kirsch-Mitzenmacher with FNV-1a 32-bit base hashes, fillRatio/falsePositiveRate/approximateCount stats, merge() for distributed set union via bitwise OR, isCompatibleWith check, toJSON/fromJSON serialization). **CountingBloomFilter** (Uint8Array counters instead of bits, supports add+remove+has, no false negatives after add/remove cycles, approximateCount via counter sum). **ScalableBloomFilter** (auto-grows with geometric capacity doubling × growthFactor + tightening error rate × tighteningRatio per layer, multi-layer union lookup, numLayers/byteSize). CLI (create/add/has/info/demo, --capacity/--error/--type/--out/--json). 34 tests, zero deps. GitHub: https://github.com/sulthonzh/bloom-filter.

- [x] **debloat** (2026-06-15) — Zero-dep AI Dependency Bloat Detector & Fixer CLI. Detects functional overlap (moment+date-fns, zustand+jotai), built-in replacements (axios→fetch, lodash→native), hallucination detection. Auto-fix mode with safe suggestions, JSON output, verbose reporting. Zero dependencies, comprehensive test suite, full documentation. GitHub: https://github.com/sulthonzh/debloat.

## Ideas / Backlog