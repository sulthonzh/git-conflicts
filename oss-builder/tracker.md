- [x] **validatekit** (2026-06-15) — Zero-dep validation library. 100+ validators: type checks (30+: null/undefined/string/number/date/map/set/promise/class/async/arrow/generator/iterable/constructor/typedArray/arrayBuffer/dataView/plainObject), empty/truthy, number checks (integer/safe/float/NaN/finite/positive/negative/nonNeg/nonPos/zero/even/odd/prime/divisibleBy/inRange/between/powerOfTwo/perfectSquare), string checks (alpha/alphanumeric/numeric/alphaDash/lowerCase/upperCase/capitalized/palindrome/slug/hex/base64/base64URL/base32/ascii/multibyte/minLength/maxLength/length with currying), format checks (email/url/ipv4/ipv6/ip/port/uuid/uuidv4/uuidv7/hexColor/rgbColor/hslColor/color/json/isoString/timestamp/creditCard-Luhn/isbn-10-13/issn/ean/macAddress/imei/iban-97/jwt/semVer/mimeType/dataUri/phoneNumber/postalCode-10-countries/currency/latitude/longitude), composition (compose/anyOf/allOf/noneOf/oneOf/optional/nullable/withDefault/not/validate-schema). Currying for multi-arg validators. CLI (check/list/demo). 110 tests, zero deps.

- [x] **trie-x** (2026-06-15) — Zero-dep trie (prefix tree). insert/search/startsWith with O(L) per op, insertAll chaining, remove with automatic dead-node pruning (keeps shared prefix nodes), autocomplete (sorted results + optional limit), wildcard pattern matching (? = single char, * = any sequence including empty — both combinable), longestPrefixOf (longest stored word that is prefix of input — for tokenizers/routers/input completion), getAllWords, countNodes, size/isEmpty/clear, toJSON/fromJSON serialization (compact {e,c} format), Trie.from() static factory. CLI (insert/search/prefix/complete/wildcard/longest/words/info/demo, stdin JSON array or newline-delimited, --limit/--json). 37 tests, zero deps.

# OSS Builder Tracker

- [x] **graphlib-x** (2026-06-15) — Zero-dep graph algorithms library. Directed/undirected weighted graphs (Graph class: add/remove nodes+edges, neighbors, degree/inDegree/outDegree, clone, toJSON/fromJSON). 20+ algorithms: **Traversal**: BFS (distances + parent tracking, visit callback skip), DFS (discover/finish timestamps), dfsAll (all components), reconstructPath. **Shortest paths**: Dijkstra (linear min-scan), shortestPath, Bellman-Ford (negative weights + negative cycle detection), Floyd-Warshall (all-pairs O(V³)), A* (custom heuristic). **Topological sort**: Kahn's (BFS in-degree) + DFS-based, cycle → null. **Cycle detection**: directed (3-color DFS), undirected (union-find), findCycle (reconstructs cycle path). **Components**: connectedComponents (BFS, weakly connected for directed), stronglyConnectedComponents (Tarjan's single-pass). **MST**: kruskalMST (union-find), primMST (greedy nearest). **Max flow**: Ford-Fulkerson / Edmonds-Karp (BFS augmenting paths, residual capacities). **Bipartite**: isBipartite (2-coloring BFS) with coloring map. **Centrality**: betweennessCentrality (Brandes' O(VE) algorithm). CLI (demo/info/bfs/dfs/topo/cycle/components/scc/dijkstra/path/mst/flow/bipartite/centrality, --input/--json). 46 tests, zero deps.

- [x] **bloom-filter** (2026-06-15) — Zero-dep probabilistic set membership testing. Three variants: **BloomFilter** (standard, bit-packed Uint8Array with optimal sizing from capacity+errorRate, double hashing via Kirsch-Mitzenmacher with FNV-1a 32-bit base hashes, fillRatio/falsePositiveRate/approximateCount stats, merge() for distributed set union via bitwise OR, isCompatibleWith check, toJSON/fromJSON serialization). **CountingBloomFilter** (Uint8Array counters instead of bits, supports add+remove+has, no false negatives after add/remove cycles, approximateCount via counter sum). **ScalableBloomFilter** (auto-grows with geometric capacity doubling × growthFactor + tightening error rate × tighteningRatio per layer, multi-layer union lookup, numLayers/byteSize). CLI (create/add/has/info/demo, --capacity/--error/--type/--out/--json). 34 tests, zero deps.

- [x] **merge-deep** (2026-06-14) — Zero-dep deep merge for objects and arrays. mergeDeep (variadic), merge (two-arg), mergeAll (array), all with options. 4 array strategies: replace (source wins), concat (append), merge (index-by-index deep merge, extras appended), unique (dedup concat). Circular reference detection via WeakSet tracking (skip returns target as-is, error throws with path). Immutable by default (clone=true), opt out with clone=false. Custom onConflict(target, source, key, path) handler for per-key resolution. Symbol keys preserved via Reflect.ownKeys. isEqual(a, b) deep equality bonus. CLI with file/stdin/overlay input, --array strategy, --pretty, --mutable. 34 tests, zero deps.

- [x] **idkit** (2026-06-14) — Zero-dep ID generation: UUID v4 (RFC 4122 random), UUID v7 (RFC 9562 time-ordered with 48-bit timestamp + extraction), ULID (Crockford Base32, lexicographically sortable + timestamp extraction), NanoID (configurable alphabet/size, bitmask bias elimination), Snowflake (Twitter-style 64-bit distributed, custom bit allocation, machine config, decode), Cuid2 (timestamp+counter+fingerprint+random, collision-resistant), batch() multi-gen, isValid() format validator (uuid/uuidv4/uuidv7/ulid/nanoid). CLI (uuid4/uuid7/ulid/nano/cuid/snowflake/batch/valid, --size/--machine/--epoch/--json). 53 tests, zero deps.

- [x] **hashring-x** (2026-06-14) — Zero-dep consistent hashing. HashRing with virtual nodes (150 default) for even key distribution, O(log N) binary search ring lookups, add/remove/addAll chainable methods, get(key) for primary node, getReplicas(key, n) for multi-replica writes (walks ring collecting distinct nodes), moves(newNode, keys) for migration planning (predicts which keys move where), distribution(samples) for spread analysis, stddev() for balance metric, more-vnodes-better-distribution property, custom hash function support, estimateVnodes() heuristic. CLI (get/replicas/dist/moves/vnodes/hash, --nodes/--vnodes/--samples/--target/--keys/--json). 30 tests, zero deps.

- [x] **fsm-x** (2026-06-15) — Zero-dep finite state machine library. Hierarchical (nested) states with automatic initial-child resolution, guarded transitions (boolean predicates), entry/exit/transition actions (side-effect callbacks with setContext/setState), internal transitions (no target = action-only, no state change), wildcard `*` event matching, final states (type:'final' auto-stops machine), lifecycle hooks (on('transition'/'state'/'event'/'error') with unsubscribe), availableEvents() (collects from current state up ancestor chain), can() (checks transition existence + guard evaluation), matches() (hierarchical prefix: matches('active') true when in 'active.running'), history states (.H shallow / .H* deep), toJSON/fromJSON persistence, Mermaid stateDiagram-v2 export + Graphviz DOT export. CLI (run/events/info/mermaid/dot/demo). 60 tests, zero deps.

- [x] **avl-tree-x** (2026-06-15) — Zero-dep AVL self-balancing BST. Insert/delete/search O(log n) with automatic rebalancing (LL, RR, LR, RL rotations). Range queries [low, high], predecessor/successor, rank (count < target), select (k-th smallest). Traversals: in-order, pre-order, post-order, level-order (BFS). Symbol.iterator for for-of. Custom comparators (numbers, strings, objects). toJSON/fromJSON serialization. isValid() verifies BST + AVL properties + height consistency. CLI (insert/search/range/rank/select/order/info/demo, --json). 50 tests, zero deps.

## Shipped
- [x] **throttle-x** (2026-06-14) — Zero-dep rate-control utilities: debounce (leading/trailing/cancel/flush/pending, AbortSignal), debounceAsync (shared result for concurrent callers), throttle (leading/trailing/cancel/flush/pending, AbortSignal), throttleAsync (in-flight promise sharing), once (single execution + reset), memoize (custom resolver, cache Map, clear), delay/sleep (abortable promise sleep), timeout (promise race with TimeoutError). CLI with demos. 40 tests, zero deps.
- [x] **diff-quick** (2026-06-14) — Zero-dep Myers O(ND) diff algorithm. myers() shortest edit script, diffLines/diffChars/diffWords/diffSentences, diffJson (RFC 6902-style structural patches), unifiedDiff with configurable context, createPatch/applyPatch round-trip, LCS via DP, diffStats, formatColor/formatPlain/formatHtml formatters, CLI (lines/chars/words/sentences/unified/json/stats/apply, --format/--from/--to/--context). 73 tests, zero deps.
- [x] **string-similarity** (2026-06-14) — Zero-dep string similarity algorithms. Levenshtein (single-row DP) + ratio, Damerau-Levenshtein (OSA), Hamming, Jaro (match window + transposition), Jaro-Winkler (prefix bonus, configurable), Sørensen-Dice (bigram), N-gram Jaccard (configurable n), Cosine (char-frequency), Soundex (H/W transparent), Metaphone (simplified). compare() unified API, findClosest/rankMatches for fuzzy lookups. CLI (compare/closest/rank/distance/soundex/metaphone, --json/--algo). 57 tests, zero deps.
- [x] **promise-pool** (2026-06-14) — Zero-dep async concurrency control. pool.map/filter/each with concurrency limit, PromisePool class (reusable, per-call overrides), per-task timeout, AbortSignal support, stopOnError + collectErrors, FIFO/LIFO scheduling, progress callback, TaskError with index/item/cause. CLI (map/filter/each/info, --json). 34 tests, zero deps.
- [x] **ratelimit-mini** (2026-06-14) — Zero-dep rate limiting with 5 algorithms: Token Bucket (continuous refill), Fixed Window (epoch-aligned), Sliding Window (precision buckets), Sliding Log (exact per-request), Leaky Bucket (steady outflow). Unified RateLimiter wrapper, KeyedRateLimiter for per-key isolation with maxSize eviction. CLI (test/info, --json). 46 tests, zero deps.
- [x] **colorkit-quick** (2026-06-14) — Zero-dep color manipulation. RGB/HSL/HSV conversions, parse hex/rgb()/hsl(), lighten/darken/saturate/desaturate/rotate/invert/grayscale, mix with alpha, WCAG contrast + AA/AAA grades + readableText, harmonies (5 types), gradient + gradientStops (multi-stop), brightness/isDark/isLight/isWarm, format to hex/rgb/hsl/hsv, CLI with --json. 81 tests, zero deps.
- [x] **lrucache-x** (2026-06-14) — Zero-dep LRU cache with TTL, eviction callbacks, and stats. Map insertion order for O(1) recency. Per-entry + default TTL (lazy + eager purge), onEvict callback with reason, stats tracking (hitRate etc), peek (no recency update), getOrSet memoization. 32 tests, zero deps.
- [x] **humanize-quick** (2026-06-14) — Zero-dep human-readable formatting. bytes (decimal/binary), duration (y→ms with maxParts), relativeTime (past/future, irregular plurals), ordinal (correct teen rules), compactNumber (K/M/B/T/Q), pluralize (custom plural), list (Oxford comma, custom conjunction), percentage, pad, truncate, clock. CLI with --json. 80 tests, zero deps.
- [x] **fuzzyfind** (2026-06-14) — Zero-dep fuzzy string matching with fzf/Sublime-style scoring. DP-based optimal positioning, word boundary + camelCase + consecutive + uppercase bonuses, gap/trailing penalties. match/isMatch/filter/highlight/highlightRanges API. CLI with stdin/file/list input, --json/--limit/--no-color/--score-only. 47 tests, zero deps.
- [x] **pathmatch** (2026-06-14) — Zero-dep URL path pattern matching. Compile (:name, :name?, :name(regex), *, (a|b)), match+extract params, build URLs, router with specificity ranking (static > param > wildcard), CLI (match/params/build/test/rank/routes). 49 tests, zero deps.
- [x] **datemath** (2026-06-14) — Zero-dep date math expression parser. Arithmetic (add/sub ms→years), snap to boundaries (sec→year, start/end), business days with custom weekends+holidays, pipe chaining (`now | + 10 days | / month`), keywords (today/tomorrow/yesterday), functions (business_days/start_of/end_of/add/min/max), quoted date strings, month overflow handling. CLI with --format/--ref/--weekend/--json. 81 tests, zero deps.
- [x] **globmatch** (2026-06-14) — Zero-dep glob pattern matcher. *, **, ?, [abc], [a-z], [!abc], {a,b,c} brace expansion (nested), ! negation, | alternation. Options: nocase/noglobstar/dot. Library + CLI (stdin/file/list, --json, --invert). 49 tests, zero deps.
- [x] **semver-tool** (2026-06-14) — Zero-dep SemVer 2.0.0 parser, comparator, bumper, range checker. Parse/valid/coerce, full prerelease precedence (§11), caret/tilde/x-range/hyphen/OR ranges, maxSatisfying, diff, sort. CLI with all ops. 90 tests, zero deps.
- [x] **jsonpatch** (2026-06-14) — RFC 6902 JSON Patch. All 6 ops (add/remove/replace/move/copy/test), JSON Pointer RFC 6901, diff engine with prefix/suffix array matching, immutable by default, CLI with apply/diff/validate. 70 tests, zero deps.
- [x] **conflictdet** (2026-06-13) — Detect git merge conflict markers in source files. Standard markers, malformed detection, smart dir scanning, CI mode, JSON/compact output. 23 tests, zero deps.
- [x] **tap-report** (2026-06-13) — Parse TAP output into clean summaries. TAP 13/14: ok/not ok, plans, SKIP/TODO directives, YAML diagnostics, bail out, pragmas, subtest-aware. summarize()/toJSON()/checkThreshold() CI policy engine. CLI with --json/--ci/--compact/threshold flags. 65 tests, zero deps.
- [x] **portcheck** (2026-06-13) — TCP port scanner. Single/batched/range specs, concurrent scanning, service ID for 30+ ports, CI mode with --require/--forbid, JSON + compact output. 39 tests, zero deps.
- [x] **mdtoc** (2026-06-13) — Generate table of contents from markdown files. ATX + setext headings, code block aware, slug dedup (github/gitlab/bitbucket), in-place insertion with markers, JSON output, stdin, custom bullets/depth/title. 47 tests, zero deps.
- [x] **secret-scan** (2026-06-13) — Scan files for hardcoded secrets. 19 patterns: AWS, GitHub, Google, Slack, Stripe, NPM, Twilio, JWT, private keys, DB URLs, generic API keys/passwords. Severity levels, allowlist filtering, CI mode, JSON output. 54 tests, zero deps.
- [x] **lockfile-diff** (2026-06-13) — Diff two npm/yarn/pnpm lockfiles. Supports package-lock.json (v1/v2/v3), yarn.lock (berry), pnpm-lock.yaml (v9). Cross-format diffing, semver change classification (major/minor/patch/prerelease), CI mode, JSON output. 46 tests, zero deps.
- [x] **tree-diff** (2026-06-13) — Compare two directory trees. Added/removed/modified/moved detection, SHA-256 hashing, move detection via hash matching, ext/glob filtering, CI mode, size stats. 38 tests, zero deps.
- [x] **tsbound** (2026-06-13) — TypeScript import boundary analyzer. Circular dep detection via DFS with dedup, coupling metrics (afferent/efferent/instability), hotspot ranking, orphan detection, TS .js convention, CLI. 45 tests, zero deps.
- [x] **jsonschema-tomarkdown** (2026-06-13) — Generate markdown docs from JSON Schema files. Property tables, constraints, $ref resolution, allOf/anyOf/oneOf, nested properties, examples, CLI. 57 tests, zero deps.
- [x] **skillguard** (2026-06-13) — Validate AI skill/agent definitions. JSON/YAML + markdown (SKILL.md) support. 54 tests, zero deps.
- [x] **tsdown** (2026-06-13) — TypeScript compilation time analyzer per file. Severity classification, bar charts, grouping by ext/dir, JSON output, CI mode. 23 tests, zero deps.
- [x] **dep-drift** (2026-06-13) — Detect dependency drift between package.json and node_modules. All semver range types, drift severity, CI mode, JSON output. 34 tests, zero deps.
- [x] **convlog** (2026-06-13) — Generate CHANGELOGs from conventional git commits. Types/scopes/breaking, semver bumping, scope grouping, stats mode, JSON output. 27 tests, zero deps.
- [x] **typecover** (2026-06-13) — TypeScript type coverage analyzer. 10 unsafe patterns, severity levels, letter grades, CI mode, JSON output. 44 tests, zero deps.
- [x] **deadcode-hunter** (2026-06-13) — Find unused JS/TS exports. Named/default, export lists, CJS require, dynamic import, re-exports. 39 tests, zero deps.
- [x] **git-metrics** (2026-06-13) — Git repo contributor metrics, bus factor, code churn, file ownership, commit timeline. 49 tests, zero deps.
- [x] **license-audit** (2026-06-13) — Scan npm deps for license compliance. 6 categories, policy engine, CI mode, JSON output. 37 tests, zero deps.
- [x] **depgraph-viz** (2026-06-13) — Visualize npm dependency trees as ASCII or SVG. Lockfile-aware, duplicate detection, stats. 36 tests, zero deps.
- [x] **readme-doctor** (2026-06-12) — Diagnose and score README.md. 12 health checks, letter grades A-F, CI mode, zero deps, 25 tests.
- [x] **npmsiz** (2026-06-12) — Analyze npm package bloat before publish. Extension breakdown, bar charts, bloat detection. 30 tests, zero deps.
- [x] **configdiff** (2026-06-12) — Semantic diff for config files (JSON/YAML/TOML). Cross-format comparison, 50 tests, zero deps.
- [x] **cronexpr** (2026-06-12) — Parse and validate cron expressions. Next run times, human descriptions, CLI. 25 tests, zero deps.
- [x] **envcheck** (2026-06-12) — Validate .env files against a schema. 7 types, strict mode, CLI, zero deps. 47 tests.
- [x] **dockalyze** (2026-06-12) — Dockerfile best practices analyzer. 14 rules, zero deps.
- [x] **pkgcheck** (2026-06-12) — Validate package.json health. Fields, deps, licenses, scripts, engines, entry points. Zero deps.
- [x] **local-mock-api-factory** (2026-06-12) — Zero-dep mock API server from JSON. Route params, dynamic responses, delays, CORS.
- [x] **jsonl-agent-logger** (2026-06-12) — Structured JSONL logging for AI agents. Spans, counters, LLM/tool helpers, CLI, zero deps.
- [x] **ai-code-quality-analyzer** (2026-06-11) — Code quality CLI for humans and CI.
- [x] **k8s-policy-check** (2026-06-11) — OPA/Gatekeeper Rego policy linter.

- [x] **codeowners** (2026-06-14) — Zero-dep GitHub CODEOWNERS parser, validator, path matcher. Gitignore-style patterns (*, **, ?, anchored/relative dirs), last-match-wins, owner validation (@user/@org/team/email/optional ^), parse with errors+warnings, CLI (whoowns/check/unowned/stats/validate, --json). 39 tests, zero deps.

- [x] **disjoint-set** (2026-06-14) — Zero-dep Union-Find (DSU): path compression (halving) + union by rank → O(α(n)) amortized, makeSet/add/find/findValue/union/connected, componentOf/components/setSize/componentCount/has/values, toJSON/fromJSON serialization, kruskal(n, edges) → MST with weight + connectivity check, CLI (union/connected/find/components/stats/kruskal/demo, --elements/--json). 28 tests, zero deps.

- [x] **priority-queue-x** (2026-06-14) — Zero-dep binary heap priority queue: min-heap default + maxHeap convenience, create(compare?, items?) with O(N) heapify, push/pop/peek/updateAt/removeAt/toArray/toJSON/fromJSON, merge() multiple queues, kwayMerge() K sorted arrays in O(N log K), nsmallest/nlargest top-K in O(N + K log N), CLI (push/pop/peek/nsmallest/nlargest/kway/demo, --max/--numbers/--json). 41 tests, zero deps.

## Ideas / Backlog
- [ ] `git-squash-preview` — Parse TAP output, show results/timing/failures
- [x] **iptools** (2026-06-14) — Zero-dep IPv4/IPv6 toolkit. Parse/validate/normalize (v4+v6, compressed ::, v4-mapped suffix, leading-zero rejection), BigInt conversion, CIDR operations (parse/range/contains/overlap/subnet division), address classification (private/loopback/multicast/reserved), comparison. CLI with info/range/contains/overlap/subnet/normalize/valid + --json. 44 tests, zero deps.

- [x] **retryx** (2026-06-14) — Zero-dep retry with exponential backoff. 4 jitter strategies (none/full/equal/decorrelated), retry predicates (shouldRetry), per-attempt timeouts, AbortSignal support (before start + during backoff), onRetry callback, retryable() wrapper factory, schedule() for delay visualization, computeDelay() for single-delay calc, RetryExhaustedError + RetryAbortError custom errors. 28 tests, zero deps.

- [x] **retryx** (2026-06-14) — Zero-dep retry with exponential backoff. 4 jitter strategies (none/full/equal/decorrelated), retry predicates (shouldRetry), per-attempt timeouts, AbortSignal support (before start + during backoff), onRetry callback, retryable() wrapper factory, schedule() for delay visualization, computeDelay() for single-delay calc, RetryExhaustedError + RetryAbortError custom errors. 28 tests, zero deps.

- [x] **csv-quick** (2026-06-14) — Zero-dep RFC 4180 CSV parser & serializer. State machine parser (quoted fields, escaped """, CRLF/CR/LF), custom delimiters (TSV/semicolon/pipe), parse() → arrays + parseObjects() → objects, stringify() + stringifyObjects() with auto-escaping, round-trip safe, CLI (parse/parse-objects/stringify/stringify-objects, --delimiter/--quote/--eol/--columns). 58 tests, zero deps.
